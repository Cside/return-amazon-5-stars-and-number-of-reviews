{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest --run",
    "package": "rm dist*.zip && pnpm run build && zip -r dist-\"$(jq -r .version < manifest.json  | sed 's/\\./-/g')\".zip dist"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^1.0.14",
    "typescript": "^5.0.2",
    "vite": "^4.4.5",
    "vitest": "^0.34.2"
  },
  "license": "MIT"
}
