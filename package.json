{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:chrome": "tsc && vite build",
    "build:firefox": "./scripts/build-firefox.sh",
    "test": "vitest --run",
    "package:chrome": "rm -f dist-chrome-*.zip && pnpm run build:chrome && zip -r dist-chrome-\"$(jq -r .version < manifest.json  | sed 's/\\./-/g')\".zip dist",
    "package:firefox": "./scripts/package-firefox.sh"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^1.0.14",
    "@tsconfig/strictest": "^2.0.1",
    "@types/chrome": "^0.0.243",
    "typescript": "^5.0.2",
    "vite": "^4.4.5",
    "vitest": "^0.34.2"
  },
  "license": "MIT"
}
